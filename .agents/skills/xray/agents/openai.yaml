interface:
  display_name: "Xray"
  short_description: "Repo-specific patterns and workflows for xray"
  default_prompt: "Use the xray repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true